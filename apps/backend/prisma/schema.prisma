generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Consulta {
  id         Int      @id @default(autoincrement())
  perguntaA  String
  respostaA  String
  createdAt  DateTime @default(now())
}
