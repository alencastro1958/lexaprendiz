# Arquivo de exemplo de variáveis de ambiente (NÃO COMMITAR .env real)

# Chave da OpenAI (gere uma nova e NUNCA exponha publicamente)
OPENAI_API_KEY=coloque_sua_chave_aqui

# Banco SQLite local
DATABASE_URL=file:./dev.db

# Porta opcional (se quiser sobrescrever a padrão 3000)
# PORT=3000

# Observações:
# 1. Copie este arquivo para .env e preencha valores reais.
# 2. Não suba .env para o Git (já está no .gitignore).
# 3. Caso uma chave tenha sido exposta, revogue e gere outra imediatamente.