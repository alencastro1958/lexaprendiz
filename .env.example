# Exemplo de .env — NUNCA commitar um .env real
# Copie para .env e preencha os valores conforme seu ambiente

# Flask
SECRET_KEY=troque-por-um-valor-seguro
FLASK_ENV=development
FLASK_APP=app.py

# OpenAI
OPENAI_API_KEY=
OPENAI_MODEL=gpt-3.5-turbo
OPENAI_MAX_TOKENS=500

# Banco de Dados
# Para produção (Render), defina no painel como env var
# Para uso local com Postgres, descomente e ajuste:
# DATABASE_URL=postgresql+psycopg2://usuario:senha@localhost:5432/seubanco
